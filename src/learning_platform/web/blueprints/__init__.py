"""HTTP blueprints.

One blueprint per bounded surface, registered explicitly by the factory. Blueprints
stay small: a large blueprint is a sign that a surface needs splitting, not that the
file needs scrolling.
"""
